<% if(typeof errors !== 'undefined' && errors && errors.length>0) { %>
<div class="error-box">
	<strong>
		Error!
	</strong>
	<ul>
		<% errors.forEach((error) => { %>
			<li><%= error %></li>
		<% }) %>
	</ul>
</div>
<% } %>